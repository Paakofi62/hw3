class SquareRoot:

    @staticmethod
    def squareroot(radicand, degree):
        return (radicand ** (1 / degree))


import math
math.sqrt(9)
'this is an example of abstraction because instead of making the SquareRoot function, you call just import the '
'function and show only the relevant operations like the math.sqrt() operation'

